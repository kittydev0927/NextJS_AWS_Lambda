import { styled } from '@mui/material';

export const StyledFormProgress = styled('div')`
  .MuiStep-root {
    .step-label-con {
      padding-left: 0;
    }
  }
  .stepper {
    padding: 16px;
  }
`;
