interface HomeNoLongerAvailableModalProps {
  openModal?: boolean;
  backdropStyles?: Record<string, unknown>;
  onModalCloseCallback?: () => void; //method will be called on modal close
}

export default HomeNoLongerAvailableModalProps;
